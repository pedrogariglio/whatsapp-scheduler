# Session 2026-05-28

## Objetivos
- Completar la validacion real de Docker en el servidor.
- Resolver cualquier bloqueo operativo que impidiera la persistencia de la sesion.
- Terminar la sesion con la app corriendo en Docker como servicio principal.

## Cambios realizados
- Se ejecuto preflight operativo en el servidor.
- Se genero un backup manual de `STATE_DIR` antes de la migracion.
- Se valido Docker manualmente en puerto temporal `10.0.0.1:3301`.
- Se reenrolo la cuenta de WhatsApp en Docker al confirmar que la sesion previa ya no era valida.
- Se valido la UI en Docker con:
  - login OK
  - testigo verde
  - mensaje programado enviado correctamente
- Se detecto un problema de persistencia al reiniciar el contenedor:
  - `profile appears to be in use by another Chromium process on another computer`
- Se aplico fix en `compose.yml` para fijar `hostname: whatsapp-scheduler`.
- Se limpiaron locks stale `Singleton*` y `Lock*` dentro de `.wwebjs_auth`.
- Se revalido la persistencia en Docker:
  - sin QR nuevo
  - con `Sesion guardada`
  - con `WhatsApp Web listo para enviar mensajes`
- Se actualizo el `.env` del servidor con `PANEL_BIND=10.0.0.1`.
- Se realizo el cutover final de `systemd` a `whatsapp-scheduler-docker.service`.

## Decisiones importantes
- Mantener `whatsapp-scheduler-docker.service` como servicio principal.
- Publicar el panel solo sobre WireGuard mediante `PANEL_BIND=10.0.0.1`.
- Fijar hostname estable del contenedor para evitar falsos locks del perfil Chromium al recrear Docker.
- No borrar `.wwebjs_auth` completa; solo limpiar locks stale cuando sea necesario.

## Problemas encontrados
- La cuenta de WhatsApp ya no estaba vinculada, por lo que el primer QR observado era consistente con sesion inexistente o invalida.
- En el reinicio controlado de Docker aparecio un bloqueo de perfil Chromium asociado a hostname variable del contenedor.
- El `.env` del servidor no reflejaba automaticamente `PANEL_BIND` porque `.env` no esta versionado en git.

## Validacion final
- `whatsapp-scheduler-docker.service` quedo habilitado y activo.
- `docker compose ps` mostro `10.0.0.1:3001->3001/tcp`.
- Los logs confirmaron:
  - `Autenticado correctamente. Sesion guardada.`
  - `WhatsApp Web listo para enviar mensajes.`
- Desde la workstation:
  - `http://10.0.0.1:3001/` redirige a `login.html`
  - login OK
  - testigo verde
  - mensaje programado enviado con exito

## Proximos pasos
- Validar reboot completo con Docker como servicio principal.
- Confirmar si ya no quedan dependencias operativas del host legacy antes de retirar Chromium Snap.
- Asegurar permisos restrictivos sobre `STATE_DIR`, `.env` y backups.
- Definir rotacion de credenciales del panel.
- Definir procedimiento controlado de reenrolamiento de WhatsApp.
