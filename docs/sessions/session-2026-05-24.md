# Session 2026-05-24

## Objetivos
- Mantener continuidad exacta del proyecto sin asumir pasos remotos no ejecutados.
- Dejar documentado el estado real antes de retomar la validacion Docker en servidor.
- Registrar el bloqueo operativo de acceso al servidor para continuar manana sin ambiguedad.

## Cambios realizados
- Se actualizo `docs/SESSION_LOG.md` con el cierre de la sesion y el punto exacto de retome.
- Se actualizo `docs/TODO.md` para reflejar el bloqueo actual y mantener priorizados los pasos de validacion Docker.
- Se dejo asentado que hoy no se ejecutaron comandos operativos sobre el servidor.
- Se dejo asentado que no hubo cambios de codigo ni cambios sobre el estado productivo.

## Decisiones importantes
- No asumir validaciones remotas que no fueron ejecutadas.
- Mantener como checkpoint funcional vigente el despliegue host con `whatsapp-scheduler.service`.
- Mantener como estrategia de prueba Docker el uso de `PANEL_BIND=10.0.0.1` y `PORT=3301` para evitar conflicto con el puerto `3001` del servicio host.
- Posponer cualquier migracion de `systemd` hasta completar las cuatro validaciones funcionales:
  - WhatsApp llega a `ready`
  - panel accesible por WireGuard
  - login correcto y testigo verde
  - mensaje de prueba en estado `Sent`

## Problemas encontrados
- No hubo acceso operativo al servidor durante esta sesion.
- Los intentos de diagnostico remoto por SSH usando el alias local `minipc` terminaron en timeout hacia `192.168.18.29:22`.
- Por ese bloqueo no fue posible ejecutar:
  - `git pull` en el servidor
  - preflight operativo
  - backup manual previo
  - prueba Docker en `3301`
  - validacion funcional real
  - migracion final a Docker

## Proximos pasos
- Hacer `git push` desde la workstation.
- En el servidor, ejecutar `git pull`.
- Correr el preflight documentado en `docs/DEPLOYMENT.md`.
- Crear backup manual con `npm run backup:state`.
- Levantar Docker manualmente con `PANEL_BIND=10.0.0.1 PORT=3301 docker compose up -d --build`.
- Seguir logs hasta confirmar `WhatsApp Web listo para enviar mensajes` sin nuevo QR.
- Validar panel por WireGuard, login, testigo verde y envio de prueba con estado `Sent`.
- Solo despues de esas validaciones, migrar a `whatsapp-scheduler-docker.service`.
