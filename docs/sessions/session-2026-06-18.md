# Session 2026-06-18

## Cambios realizados
- Se ejecuto una sesion completa en modo tutor, con comandos operativos corridos por el usuario paso a paso.
- Se confirmo el punto de ejecucion productivo real en `WorkingDirectory=/home/pedrogariglio/whatsapp-scheduler` con `docker compose`.
- Se verifico estado previo de autenticacion:
  - `auth.username` presente
  - `auth.passwordHash` presente
  - `firstRun=false`
  - `ALLOW_LOCAL_WEB_SETUP=false`
  - `SESSION_SECRET_LEN=64`
- Se valido rotacion normal de credenciales del panel con `npm run setup-admin` dentro del contenedor.
- Se comprobo el cambio real de credenciales comparando fingerprint SHA-256 del `passwordHash` antes y despues.
- Se valido login exitoso con credenciales nuevas y acceso autenticado a `/health`.
- Se valido rotacion forzada:
  - reinicio de contenedor luego de rotar credenciales
  - invalidacion de sesion activa previa (`401 Unauthorized` con cookie vieja)
  - nuevo login exitoso con credenciales rotadas (`HTTP=200`)
- Durante la rotacion forzada, WhatsApp quedo fuera de sesion y entro en loop de QR.
- Se detecto evento `Cliente desconectado: LOGOUT` con fallo puntual al intentar borrar `/state/.wwebjs_auth` desde la app.
- Se aplico recuperacion operativa controlada:
  - detener contenedor
  - borrar manualmente `/opt/whatsapp-scheduler/state/.wwebjs_auth`
  - levantar contenedor
  - reenrolar por QR desde el telefono
- Se confirmo recuperacion completa:
  - `Autenticado correctamente. Sesion guardada.`
  - `WhatsApp Web listo para enviar mensajes.`
  - `HEALTH_FINAL {"ok":true,"whatsappReady":true}` con `HTTP=200`
- Se verifico que en los ultimos 10 minutos no reaparecieron `LOGOUT`, `auth_failure` ni errores de borrado de `.wwebjs_auth`.

## Decisiones importantes
- Quedo definido y validado el procedimiento de rotacion de credenciales del panel en dos variantes:
  - rotacion normal sin reinicio
  - rotacion forzada con reinicio para invalidar sesiones activas
- Quedo definido y validado un procedimiento controlado de reenrolamiento de WhatsApp cuando la sesion queda invalida tras una operacion de seguridad.
- Se mantuvo el enfoque de no asumir resultados no verificados: cada decision se tomo sobre evidencia de salida real de comandos.

## Problemas encontrados
- Primer intento de comando fallido por ejecutar `docker compose` fuera del `WorkingDirectory` del servicio productivo.
- Primer intento de `setup-admin` rechazado por politica de contrasena minima (< 10 caracteres).
- Tras reinicio de la rotacion forzada, hubo ventana temporal de no disponibilidad (`HTTP=000`) hasta que el contenedor termino de arrancar.
- La sesion de WhatsApp se invalido durante la prueba forzada y aparecio loop de QR.
- La app reporto una falla puntual al intentar borrar `/state/.wwebjs_auth` despues de `LOGOUT`.

## Proximos pasos
- Resolver el `headless boot issue` del HP EliteDesk para permitir reboots remotos seguros sin monitor.
- Evaluar limites operativos adicionales para `systemd` si no afectan Chromium headless ni Docker.
- Verificar que uploads no queden persistidos tras fallos inesperados.
- Definir un chequeo simple de salud y monitoreo minimo para estado del panel y `whatsappReady`.
